To install the kinetik-cli:

```
    export HOMEBREW_GITHUB_API_TOKEN=your persontal github token
    brew tap xiaoanne/kinetik-cli https://github.com/xiaoanne/homebrew-kinetik-cli.git
    brew install kinetik-cli
```